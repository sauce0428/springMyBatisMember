<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zeus.mapper.MemberMapper">
	<!-- 외부 조인을 사용하여 얻은 검색 결과에 대한 매핑을 정의한다. -->
	<resultMap type="Member" id="memberMap">
		<id property="no" column="no" />
		<result property="no" column="no" />
		<result property="id" column="id" />
		<result property="pw" column="pw" />
		<result property="name" column="name" />
		<result property="regDate" column="regDate" />
		<result property="modDate" column="moddate" />
		<collection property="authList" resultMap="authMap">
		</collection>
	</resultMap>

	<resultMap type="MemberAuth" id="authMap">
		<result property="no" column="no" />
		<result property="auth" column="auth" />
	</resultMap>

	<!-- 부모 객체와 관계 객체를 작성하는 데 필요한 모든 레코드를 검색하도록 외부 조인을사용한 SQL을 정의한다. -->
	<select id="read" resultMap="memberMap">
		SELECT m.no, m.id, m.pw, m.name, m.regdate, m.moddate, a.auth
		FROM mybatisMember m INNER JOIN mybatismember_auth a ON
		m.no = a.no
		WHERE m.no = #{no}
	</select>

	<select id="list" resultType="Member">
		SELECT no, id, pw, name, regDate FROM mybatisMember	ORDER BY regDate DESC
	</select>

	<update id="update">
		UPDATE mybatisMember
		SET name =#{name}, pw = #{pw}
		WHERE no = #{no}
	</update>

	<delete id="delete">
		DELETE FROM mybatisMember
		WHERE no = #{no}
	</delete>

	<delete id="deleteAuth">
		DELETE FROM mybatismember_auth
		WHERE no = #{no}
	</delete>

	<!-- 데이터베이스에서 기본키를 인수로 전달한 자바빈의 프로퍼티를 통해 취득한다. -->
	<!-- <insert id="create" useGeneratedKeys="true" keyProperty="userNo"> -->
	<insert id="create" parameterType="Member">
		<selectKey keyProperty="no" resultType="int" order="BEFORE">
			select mybatismember_seq.NEXTVAL FROM DUAL
		</selectKey>
		INSERT INTO mybatisMember (no, id, pw, name)
		VALUES(#{no}, #{id}, #{pw}, #{name})
	</insert>

	<insert id="createAuth">
		INSERT INTO mybatismember_auth(no, auth)
		VALUES(#{no}, #{auth})
	</insert>
</mapper>


